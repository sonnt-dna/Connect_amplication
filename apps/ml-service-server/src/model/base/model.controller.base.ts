/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { ModelService } from "../model.service";
import { ModelCreateInput } from "./ModelCreateInput";
import { Model } from "./Model";
import { ModelFindManyArgs } from "./ModelFindManyArgs";
import { ModelWhereUniqueInput } from "./ModelWhereUniqueInput";
import { ModelUpdateInput } from "./ModelUpdateInput";
import { TrainingJobFindManyArgs } from "../../trainingJob/base/TrainingJobFindManyArgs";
import { TrainingJob } from "../../trainingJob/base/TrainingJob";
import { TrainingJobWhereUniqueInput } from "../../trainingJob/base/TrainingJobWhereUniqueInput";
import { PredictionFindManyArgs } from "../../prediction/base/PredictionFindManyArgs";
import { Prediction } from "../../prediction/base/Prediction";
import { PredictionWhereUniqueInput } from "../../prediction/base/PredictionWhereUniqueInput";

export class ModelControllerBase {
  constructor(protected readonly service: ModelService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: Model })
  async createModel(@common.Body() data: ModelCreateInput): Promise<Model> {
    return await this.service.createModel({
      data: data,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        status: true,
        accuracy: true,
        name: true,
        version: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [Model] })
  @ApiNestedQuery(ModelFindManyArgs)
  async models(@common.Req() request: Request): Promise<Model[]> {
    const args = plainToClass(ModelFindManyArgs, request.query);
    return this.service.models({
      ...args,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        status: true,
        accuracy: true,
        name: true,
        version: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: Model })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async model(
    @common.Param() params: ModelWhereUniqueInput
  ): Promise<Model | null> {
    const result = await this.service.model({
      where: params,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        status: true,
        accuracy: true,
        name: true,
        version: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: Model })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async updateModel(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() data: ModelUpdateInput
  ): Promise<Model | null> {
    try {
      return await this.service.updateModel({
        where: params,
        data: data,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          status: true,
          accuracy: true,
          name: true,
          version: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: Model })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  async deleteModel(
    @common.Param() params: ModelWhereUniqueInput
  ): Promise<Model | null> {
    try {
      return await this.service.deleteModel({
        where: params,
        select: {
          id: true,
          createdAt: true,
          updatedAt: true,
          status: true,
          accuracy: true,
          name: true,
          version: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Get("/:id/trainingJobs")
  @ApiNestedQuery(TrainingJobFindManyArgs)
  async findTrainingJobs(
    @common.Req() request: Request,
    @common.Param() params: ModelWhereUniqueInput
  ): Promise<TrainingJob[]> {
    const query = plainToClass(TrainingJobFindManyArgs, request.query);
    const results = await this.service.findTrainingJobs(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        status: true,
        endTime: true,
        startTime: true,

        model: {
          select: {
            id: true,
          },
        },

        dataset: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/trainingJobs")
  async connectTrainingJobs(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() body: TrainingJobWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trainingJobs: {
        connect: body,
      },
    };
    await this.service.updateModel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/trainingJobs")
  async updateTrainingJobs(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() body: TrainingJobWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trainingJobs: {
        set: body,
      },
    };
    await this.service.updateModel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/trainingJobs")
  async disconnectTrainingJobs(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() body: TrainingJobWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      trainingJobs: {
        disconnect: body,
      },
    };
    await this.service.updateModel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Get("/:id/predictions")
  @ApiNestedQuery(PredictionFindManyArgs)
  async findPredictions(
    @common.Req() request: Request,
    @common.Param() params: ModelWhereUniqueInput
  ): Promise<Prediction[]> {
    const query = plainToClass(PredictionFindManyArgs, request.query);
    const results = await this.service.findPredictions(params.id, {
      ...query,
      select: {
        id: true,
        createdAt: true,
        updatedAt: true,
        inputData: true,
        outputData: true,
        predictedAt: true,

        model: {
          select: {
            id: true,
          },
        },
      },
    });
    if (results === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return results;
  }

  @common.Post("/:id/predictions")
  async connectPredictions(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() body: PredictionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      predictions: {
        connect: body,
      },
    };
    await this.service.updateModel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Patch("/:id/predictions")
  async updatePredictions(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() body: PredictionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      predictions: {
        set: body,
      },
    };
    await this.service.updateModel({
      where: params,
      data,
      select: { id: true },
    });
  }

  @common.Delete("/:id/predictions")
  async disconnectPredictions(
    @common.Param() params: ModelWhereUniqueInput,
    @common.Body() body: PredictionWhereUniqueInput[]
  ): Promise<void> {
    const data = {
      predictions: {
        disconnect: body,
      },
    };
    await this.service.updateModel({
      where: params,
      data,
      select: { id: true },
    });
  }
}
